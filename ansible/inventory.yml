---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: auto
    node_1_primary:
      ansible_host: 192.168.241.45
      ansible_user: ubuntu
      ansible_python_interpreter: auto
      ansible_ssh_private_key_file: "{{ lookup('env', 'HOST_HOME') | default(lookup('env', 'HOME'), true) }}/.ssh/jenkins-master.key"
    node_2_secondary:
      ansible_host: 192.168.241.51
      ansible_user: ubuntu
      ansible_python_interpreter: auto
      ansible_ssh_private_key_file: "{{ lookup('env', 'HOST_HOME') | default(lookup('env', 'HOME'), true) }}/.ssh/jenkins-master.key"
